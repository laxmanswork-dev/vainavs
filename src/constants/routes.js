/**
 * Route path constants. Reference these instead of hardcoded strings in
 * <Link> / navigate() calls so a path rename only ever happens in one place.
 */
export const ROUTES = {
  HOME: '/',
  MENU: '/menu',
  CONTACT: '/contact',
  NOT_FOUND: '*',
}
