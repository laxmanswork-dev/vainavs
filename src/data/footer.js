import { footerCafeFront, videoFooterBg } from '@assets'

/** Ambient background layer behind `layout/Footer.jsx`. */
export const FOOTER_BACKGROUND = {
  image: footerCafeFront,
  alt: "Vainav's Cafeteria storefront",
}

/** Optional video variant — swap in via `<BackgroundVideo src={FOOTER_BACKGROUND_VIDEO} />`. */
export const FOOTER_BACKGROUND_VIDEO = videoFooterBg
